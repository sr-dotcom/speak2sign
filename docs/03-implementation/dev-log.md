# Dev log

One entry per working day: done, blocked, decided.

## 2026-09-02
- Done: execution plan, three verification reports, system design + three archify diagrams, timeline contract schema, PRD, ADR 0004, repo scaffold (folders, requirements, CI, config), spike 1 script.
- Blocked: nothing. Community Cloud deploy needs the developer to push to a public GitHub repo.
- Decided: see ADR 0004 and the ⚑ list in the execution plan; all await developer confirmation.

## 2026-09-03
- Done: local venv + dev deps; ruff clean; smoke test green. Spike 1 (CATS sample) and spike 2 (Signbank ECV, entry and video URL pattern, one clip download) completed and written to `docs/research/spikes.md`. LICENSE, NOTICE, README, CLAUDE.md, Dockerfile, app skeleton.
- Blocked: first deploy needs the developer to create the public GitHub repo and push; spike 3 needs a Kaggle session.
- Decided: CATS `.ia.mp4` derivatives used as-is (480p, ≤ 1.1 MB); letters and digits from Signbank.
- Phase 1 started (developer go-ahead 2026-09-03, Charlotte for the weather lane): live NWS forecast sampled; 39 VOA newscast transcripts pulled from the Internet Archive; anchor-read segments separated from Reuters/AP correspondent segments; six excerpts proposed in `data/demo/excerpts.json`; ASL-LEX signdata.csv downloaded (2,723 rows) for frequency ranking; `scripts/vocab_seed.py` wrote `data/lexicon/target_vocab.csv` (261 rows incl. 36 letters/digits). Awaiting developer review of both files before clip fetching.
- Lexicon builder (`scripts/build_lexicon.py`, plan/fetch modes) written; plan run over 260 concepts: 179 CATS + 81 Signbank, 0 missing. 29 automatic picks hit the wrong word sense (incl. one offensive gloss for "people") → reviewed `overrides.json`; Signbank ranking fixed to prefer the base gloss. `light` concept dropped, routed to `slight` by the sense table. `senses.json` and `tests/test_lexicon.py` added. `plan_review.csv` sent to the developer; fetch awaits go.
- Fetch done: 259 of 260 concepts have a clip (83.7 MB under static/); `money` failed with HTTP 401 from Signbank (FINANCE video is protected). Two run aborts fixed: Signbank names video files by lemma (SAYstr → SAY-536.mp4, now read from the entry page) and one archive.org item disconnects (per-concept failure handling added). Six contact sheets in `docs/research/clip-review-*.jpg`, reviewed frame by frame.
- Clip review: all 258 mid-frames eyeballed. 10 CATS clips replaced by Signbank citation forms (title cards, illustrations, a map, an avatar); `los-angeles` and `evening` concepts dropped (fingerspell / folded into `night`); 7 keyword collisions resolved; keyword variants added (past tenses, number words). All concepts `attested`. Naive coverage estimate: weather 86%, news items 64–83%, overall 74% with proper nouns counted as misses; the real metric comes from the Phase 2 rule pass.
- Rule pass (`src/speak2sign/gloss/rules.py`) + lexicon loader (`gloss/lexicon.py`) + 10 tests + `scripts/coverage_report.py` → `docs/04-testing/coverage-rules.md`. Rule-pass coverage: forecast 90%, news items 68–90%, overall 81% with 18% fingerspelled (mostly names). Numbers count as validated (digit clips are signs). 'red flag' removed as a plain keyword so the sense rule wins.
- Timeline builder (`timeline.py`), provenance module (`provenance.py`), transcript types (`transcript.py`), clip durations recorded in concepts.json (mean 3.15 s), schema gains caption `dropped` flag; contract tests validate every curated item plus typed/weather lanes against the schema; 29 tests green. `contracts/example.timeline.json` written from the California fire item. Signing time exceeds speech time roughly 2× at 1.0× clip speed: the overrun policy (ADR 0008) is going to be exercised constantly, not occasionally.
- Timing measurement (`docs/research/timing-findings.md`): signing time is 5.5–9.3× speech time even after trimming to active spans. Fingerspelling at ~1.8 s/letter is the biggest cost. ADR 0008's "catching up" model cannot absorb this; decision put to the developer.
- ADR 0008 written (interpreter-paced, active spans at 1.25×/2.0×, names once); schema gains `playback`, `sentences`, clip `in_s/out_s/rate`, badge `name`, stats `names`; timeline and provenance updated; TRD §6 rewritten; active spans measured into concepts.json (83% active); tests green.
- Weather lane: `ingest/nws.py` (Charlotte gridpoint, identifying User-Agent, gridpoint re-resolve fallback, 4 periods → sentences), offline fixture test; `ui/ribbon.py` renders captions with dropped words struck through, badge chips with notes on hover, stats line; `app.py` now has Type-text and Live-weather tabs with a 5-minute forecast cache and a no-traceback error path. 34 tests green.
- Local run checked in the browser: typed lane and live weather lane both render the ribbon end to end (forecast issued 2026-09-03 19:22 fetched live). Fixes from the check: Signbank variant suffixes hidden on screen (LEADERb → LEADER); captions keep spoken order when a time sign is fronted.
- Interpreter panel built (`ui/panel.html|css|js|py`, ~200 lines JS, no library): interpreter-paced playback, double-buffered clips playing `in_s..out_s` at `rate`, caption highlight from speechSynthesis boundary events or audio time, name/not-available shown as text cards, waiting state, Play/Pause/Restart. Checked in the browser: video signer visible, badges, pacing works. Spike 4 local part recorded. Two gotchas fixed: shadow-DOM `[hidden]` vs author `display`, and `cache_resource` needing a restart after CSS edits.
- Curated items built (`scripts/build_demo_set.py`): six VOA excerpts located in their newscasts with locate scores 0.88–1.00, word alignment 96–99% (whisper base.en int8, word timestamps), 16 kHz mono WAV under `static/news/` (5.1 MB total), timings in `data/demo/<id>.json`. Whisper transcribes 300 s of audio in ~6 s on this laptop; **peak RSS 507 MB** with the model loaded (first real number for the memory budget). Model files fetched directly with urllib because huggingface_hub's client fails behind this machine's TLS-inspecting proxy (Windows cert store works). `ingest/demo_set.py` + News items tab in the app; checked in the browser with real anchor audio driving the caption highlight. Fix: sentence boundaries now see a full stop inside closing quotes.
- Component declaration no longer cached (AppTest and reruns need it registered per run). Names-once now also catches a sentence-initial name that appears capitalised elsewhere. With real timings: coverage 68–90%, 3–4 sentences per item, signing 4–5.5× speech. 36 tests green.
- Upload lane: `asr.py` (lazy faster-whisper base.en int8, 60 s cap, in-memory decode, WAV data URL so nothing touches disk, reference/whisper alignment), Upload tab with editable transcript, `scripts/measure_rss.py` in CI (budget 1800 MB; measured peak **334 MB** for lexicon + whisper + one transcription on Windows; the earlier 507 MB figure included decoding six 5-minute newscasts). faster-whisper is now a runtime dependency (ADR 0002 rule: base.en only). 40 tests green.
- 2026-09-04: first deploy live at https://speak2sign.streamlit.app (owner signed in at share.streamlit.io); remote spike 4 passed; docs updated.
- Pre-push audit: leak scan (personal email replaced by the repo URL in User-Agents, local-path diagram artefacts ignored, no secrets), ponytail review (13 findings applied, −110 lines, demo-set rebuild byte-identical), history rebuilt as nine SDLC-ordered commits. Public repo created and pushed; CI watching. Deployment record started.
- 2026-09-04: T5 path built: `training/train_t5_gloss.py` (Kaggle: t5-small on ASLG-PC12, fixed-seed 90/5/5 split, BLEU/chrF, results.json with samples), `training/export_ct2.py` (CTranslate2 int8 + spiece.model, zip for a GitHub Release), `gloss/t5.py` runtime (ctranslate2 + sentencepiece only, lazy, release download via `T5_RELEASE_URL`, glosses resolved through the same lexicon, DESC-/X- prefixes stripped, proportional token positions), engine toggle in the sidebar shown only when the export exists. ADRs 0005–0007 written (they were referenced but missing). Training itself UNVERIFIED until the Kaggle run. 43 tests.
- T5 trained locally on the RTX 4080 (3 epochs, 17 min; test BLEU 93.7 / chrF 97.1), exported to CTranslate2 int8 (62 MB), published as release v0.5.0-t5, spike 3 closed (load 0.15 s, +86 MB, 32–71 ms). Evaluation report written: rules stay default (81% vs 68% coverage on the news items; T5 invents spellings for unseen names and swaps unknown news words for corpus words). HF_HUB_ENABLE_HF_TRANSFER=1 is set machine-wide and must be 0 for downloads here.
