# Dev log

One entry per working day: done, blocked, decided.

## 2026-09-02
- Done: execution plan, three verification reports, system design + three archify diagrams, timeline contract schema, PRD, ADR 0004, repo scaffold (folders, requirements, CI, config), spike 1 script.
- Blocked: nothing. Community Cloud deploy needs the developer to push to a public GitHub repo.
- Decided: see ADR 0004 and the ⚑ list in the execution plan; all await developer confirmation.

## 2026-09-03
- Done: local venv + dev deps; ruff clean; smoke test green. Spike 1 (CATS sample) and spike 2 (Signbank ECV, entry and video URL pattern, one clip download) completed and written to `docs/research/spikes.md`. LICENSE, NOTICE, README, CLAUDE.md, Dockerfile, app skeleton.
- Blocked: first deploy needs the developer to create the public GitHub repo and push; spike 3 needs a Kaggle session.
- Decided: CATS `.ia.mp4` derivatives used as-is (480p, ≤ 1.1 MB); letters and digits from Signbank.
- Phase 1 started (developer go-ahead 2026-09-03, Charlotte for the weather lane): live NWS forecast sampled; 39 VOA newscast transcripts pulled from the Internet Archive; anchor-read segments separated from Reuters/AP correspondent segments; six excerpts proposed in `data/demo/excerpts.json`; ASL-LEX signdata.csv downloaded (2,723 rows) for frequency ranking; `scripts/vocab_seed.py` wrote `data/lexicon/target_vocab.csv` (261 rows incl. 36 letters/digits). Awaiting developer review of both files before clip fetching.
- Lexicon builder (`scripts/build_lexicon.py`, plan/fetch modes) written; plan run over 260 concepts: 179 CATS + 81 Signbank, 0 missing. 29 automatic picks hit the wrong word sense (incl. one offensive gloss for "people") → reviewed `overrides.json`; Signbank ranking fixed to prefer the base gloss. `light` concept dropped, routed to `slight` by the sense table. `senses.json` and `tests/test_lexicon.py` added. `plan_review.csv` sent to the developer; fetch awaits go.
- Fetch done: 259 of 260 concepts have a clip (83.7 MB under static/); `money` failed with HTTP 401 from Signbank (FINANCE video is protected). Two run aborts fixed: Signbank names video files by lemma (SAYstr → SAY-536.mp4, now read from the entry page) and one archive.org item disconnects (per-concept failure handling added). Six contact sheets in `docs/research/clip-review-*.jpg`, reviewed frame by frame.
- Clip review: all 258 mid-frames eyeballed. 10 CATS clips replaced by Signbank citation forms (title cards, illustrations, a map, an avatar); `los-angeles` and `evening` concepts dropped (fingerspell / folded into `night`); 7 keyword collisions resolved; keyword variants added (past tenses, number words). All concepts `attested`. Naive coverage estimate: weather 86%, news items 64–83%, overall 74% with proper nouns counted as misses; the real metric comes from the Phase 2 rule pass.
- Rule pass (`src/speak2sign/gloss/rules.py`) + lexicon loader (`gloss/lexicon.py`) + 10 tests + `scripts/coverage_report.py` → `docs/04-testing/coverage-rules.md`. Rule-pass coverage: forecast 90%, news items 68–90%, overall 81% with 18% fingerspelled (mostly names). Numbers count as validated (digit clips are signs). 'red flag' removed as a plain keyword so the sense rule wins.
- Timeline builder (`timeline.py`), provenance module (`provenance.py`), transcript types (`transcript.py`), clip durations recorded in concepts.json (mean 3.15 s), schema gains caption `dropped` flag; contract tests validate every curated item plus typed/weather lanes against the schema; 29 tests green. `contracts/example.timeline.json` written from the California fire item. Signing time exceeds speech time roughly 2× at 1.0× clip speed: the overrun policy (ADR 0008) is going to be exercised constantly, not occasionally.
- Timing measurement (`docs/research/timing-findings.md`): signing time is 5.5–9.3× speech time even after trimming to active spans. Fingerspelling at ~1.8 s/letter is the biggest cost. ADR 0008's "catching up" model cannot absorb this; decision put to the developer.
- ADR 0008 written (interpreter-paced, active spans at 1.25×/2.0×, names once); schema gains `playback`, `sentences`, clip `in_s/out_s/rate`, badge `name`, stats `names`; timeline and provenance updated; TRD §6 rewritten; active spans measured into concepts.json (83% active); tests green.
- Weather lane: `ingest/nws.py` (Charlotte gridpoint, identifying User-Agent, gridpoint re-resolve fallback, 4 periods → sentences), offline fixture test; `ui/ribbon.py` renders captions with dropped words struck through, badge chips with notes on hover, stats line; `app.py` now has Type-text and Live-weather tabs with a 5-minute forecast cache and a no-traceback error path. 34 tests green.
- Local run checked in the browser: typed lane and live weather lane both render the ribbon end to end (forecast issued 2026-09-03 19:22 fetched live). Fixes from the check: Signbank variant suffixes hidden on screen (LEADERb → LEADER); captions keep spoken order when a time sign is fronted.
- Interpreter panel built (`ui/panel.html|css|js|py`, ~200 lines JS, no library): interpreter-paced playback, double-buffered clips playing `in_s..out_s` at `rate`, caption highlight from speechSynthesis boundary events or audio time, name/not-available shown as text cards, waiting state, Play/Pause/Restart. Checked in the browser: video signer visible, badges, pacing works. Spike 4 local part recorded. Two gotchas fixed: shadow-DOM `[hidden]` vs author `display`, and `cache_resource` needing a restart after CSS edits.
- Curated items built (`scripts/build_demo_set.py`): six VOA excerpts located in their newscasts with locate scores 0.88–1.00, word alignment 96–99% (whisper base.en int8, word timestamps), 16 kHz mono WAV under `static/news/` (5.1 MB total), timings in `data/demo/<id>.json`. Whisper transcribes 300 s of audio in ~6 s on this laptop; **peak RSS 507 MB** with the model loaded (first real number for the memory budget). Model files fetched directly with urllib because huggingface_hub's client fails behind this machine's TLS-inspecting proxy (Windows cert store works). `ingest/demo_set.py` + News items tab in the app; checked in the browser with real anchor audio driving the caption highlight. Fix: sentence boundaries now see a full stop inside closing quotes.
- Component declaration no longer cached (AppTest and reruns need it registered per run). Names-once now also catches a sentence-initial name that appears capitalised elsewhere. With real timings: coverage 68–90%, 3–4 sentences per item, signing 4–5.5× speech. 36 tests green.
- Upload lane: `asr.py` (lazy faster-whisper base.en int8, 60 s cap, in-memory decode, WAV data URL so nothing touches disk, reference/whisper alignment), Upload tab with editable transcript, `scripts/measure_rss.py` in CI (budget 1800 MB; measured peak **334 MB** for lexicon + whisper + one transcription on Windows; the earlier 507 MB figure included decoding six 5-minute newscasts). faster-whisper is now a runtime dependency (ADR 0002 rule: base.en only). 40 tests green.
- 2026-09-04: first deploy live at https://speak2sign.streamlit.app (owner signed in at share.streamlit.io); remote spike 4 passed; docs updated.
- Pre-push audit: leak scan (personal email replaced by the repo URL in User-Agents, local-path diagram artefacts ignored, no secrets), ponytail review (13 findings applied, −110 lines, demo-set rebuild byte-identical), history rebuilt as nine SDLC-ordered commits. Public repo created and pushed; CI watching. Deployment record started.
- 2026-09-04: T5 path built: `training/train_t5_gloss.py` (Kaggle: t5-small on ASLG-PC12, fixed-seed 90/5/5 split, BLEU/chrF, results.json with samples), `training/export_ct2.py` (CTranslate2 int8 + spiece.model, zip for a GitHub Release), `gloss/t5.py` runtime (ctranslate2 + sentencepiece only, lazy, release download via `T5_RELEASE_URL`, glosses resolved through the same lexicon, DESC-/X- prefixes stripped, proportional token positions), engine toggle in the sidebar shown only when the export exists. ADRs 0005–0007 written (they were referenced but missing). Training itself UNVERIFIED until the Kaggle run. 43 tests.
- T5 trained locally on the RTX 4080 (3 epochs, 17 min; test BLEU 93.7 / chrF 97.1), exported to CTranslate2 int8 (62 MB), published as release v0.5.0-t5, spike 3 closed (load 0.15 s, +86 MB, 32–71 ms). Evaluation report written: rules stay default (81% vs 68% coverage on the news items; T5 invents spellings for unseen names and swaps unknown news words for corpus words). HF_HUB_ENABLE_HF_TRANSFER=1 is set machine-wide and must be 0 for downloads here.
- Accessibility pass (`docs/04-testing/accessibility.md`): axe-core 4.10.2 → 0 violations (28 passes; `video-caption` incomplete is a documented decision); every colour pair computed ≥ 4.8:1; panel gains named regions, `role=status` live regions for pacing state and the current sign, labelled silent videos, non-focusable video elements, bold+underline on the current caption word, reduced-motion rule. Finding: Streamlit strips `role`/`aria-*` from both `st.markdown` and `st.html`, so the ribbon relies on visible text and the panel carries the accessible information. NVDA session and Safari/Firefox still UNVERIFIED.
- Test strategy written (`docs/04-testing/test-strategy.md`): layers as built (43 tests, 94% line coverage, contract + data-integrity + memory gate), requirement traceability FR-01..17, known gaps with reasons.
- Repository organisation: an audit proposed ~25 file moves; an independent senior review (fresh context) said yes to the documentation fixes and no to the moves ("reorganised the week before submission reads as retroactive tidying"); a ponytail review cut 25 moves to 0 and 5 READMEs to 2. Applied: plan §5 and TRD §3 now match the tree; `.specify/` (never used) removed and the plan says so; two orphan lexicon files deleted; stale previous-iteration citations and a dead ADR link fixed; ADR 0002 amended; `scripts/README.md` and `docs/README.md` (with ADR status index) added; generated reports carry a generated-by header.

## 2026-09-10
- Codex CLI 0.154 installed as the independent reviewer (work policy rule 5, `AGENTS.md` brief, PR template line). Dry run on commit 18c3ecc: the built-in `codex exec review` is blocked on Windows (its sandbox rejects the pwsh commands it needs), so `scripts/codex_review.sh` pipes the diff and the brief over stdin, which works. First findings: P2 "policy change has no dev-log entry" → applied (this entry); P3 "no test for the CLI commands" → rejected, a policy paragraph is not code, the recorded dry run is its validation. Verdict from Codex on that commit: fix first; addressed here.
- `scripts/codex_review.sh` hardened through four Codex review rounds on itself: untracked files were being skipped (NUL bytes lost in command substitution; P1, applied, regression test added), brief files checked before the call (applied), empty answer treated as failure (applied), staged and unstaged diffs collected separately so a staged-only change is reviewed (applied), executable mode committed (applied). Rejected with reason: merge-commit handling (trunk-based repo, no merges), a "dedicated output mechanism" for the answer (the CLI's stdout marker is the only interface it offers; exit status plus non-empty answer is the check). Three shell-level tests with a stub codex cover empty, untracked-with-space, and CLI-failure cases. AppTest smoke tests given a 20 s timeout after timing out at 3 s while Codex ran alongside.
- Whole-codebase Codex audit (three slices: runtime, tooling, tests+structure; ~100 findings, the reports are quoted in this log's commits). Batch 1, the honesty layer, applied: non-whole numbers ('-5', '12.5', '1/2', '20-30', '10:30') were being signed as bare digit sequences under a validated badge → numeric expressions stay one token and are refused whole; 'uncommon'/'unusual'/'rare' were keywords of COMMON and 'noncitizen' of CITIZEN (opposite meaning under a validated badge) → removed from the seed, vocabulary, candidates and concepts; 'news' stemmed to NEW and 'goods' to GOOD → stem stop-list; 'José' was shortened to 'jos' by an ASCII tokenizer → any-letter tokenizer, the word is refused for lack of an 'é' clip; a sense rule pointing at a concept without a clip produced a validated entry with no clip → fail closed to not_available, and lexicon.load now refuses such rules, duplicate ids and unplayable spans; captions were rebuilt from gloss entries, so a T5 omission vanished and 'It's' became 'it is' → captions are the source words verbatim, struck through when nothing signs them (rules: dropped; T5: not accounted for, matched by concept so a synonym or stem counts as covered); T5 'RAIN-COAT' could resolve to RAIN by first-word fallback → exact keyword/phrase only; 'U.S.' and 'Dr.' ended sentences → abbreviation guard; ribbon regex cut '12.5' to '12' and escaped before inserting spelling dashes → fixed; clips carry their licence; T5 items attribute the model and ASLG-PC12; unknown attribution keys and engine names raise; signing_s counts the text holds; schema requires every field the panel reads, forbids clips on name/not_available, requires a url for audio, and drops the never-implemented 'video' kind, 'free-running' mode and 'wikimedia' source; `scripts/example_timeline.py` regenerates the example. Tests: 76, with clip identity checked against the lexicon record, sentence membership, verbatim captions, adversarial T5 output, exact digit sequences, lexicon load refusals, WAV decoding, the 60 s cap on both sides, an upload timeline without the model, NWS fallback and failure. Rejected from the audit with reason: runtime semantic validation before mounting (the contract test covers it; a runtime jsonschema dependency would only re-run the same checks), 'stats.tokens' rename (documented instead), exact T5 token positions (proportional by design, ADR 0005). Codex then reviewed this batch eleven times before the commit; each round found a real hole in the same theme (a displayed number or word differing from what plays) and each was closed with a test: '1/2', '.5', '(-5)', '1e3', '1e-3', '1.5e-3', '$1.5e-3', '− 5' and '- 5' as two words, Unicode minus and en dash, combining accents NFC cannot fold, a T5 gloss covering every repeat of a word, T5 phrase glosses, T5 same-word-other-sense, T5 '72' or 'TWO HUNDRED' for a source that says 27, a bare 'X-' marker, 'partly' captions naming the unsigned part, caption sentence ownership (so an edited upload's extra sentences keep their captions), leading punctuation, an incomplete T5 export counting as installed, a schema that let audio omit its url or 'partly' omit 'missing'. Rejected: browser-level tests for the panel (no browser test harness in the project; adding Playwright is a new dependency and CI stage, the developer's call under rule 4; the panel is checked by hand in the browser and recorded here). Round twelve did not run: the Codex usage limit was reached at 17:53 (resets 21:53); the commit says so.
- Batch 2, the panel and the page (Codex audit findings on `panel.js`, `app.py`, `asr.py`): the sign label used to change before its clip was on screen, so the previous sign's last frame sat under a new word → the label now changes when the first clip of the entry is showing; Pause left clip and audio listeners alive and a late `loadedmetadata` could restart playback → every clip and narration has a cancel function that Pause, Restart and unmount call, and every step re-checks the run token; a missing or undecodable clip hung the panel for ever → 8 s load timeout and `error` handler, the sign is skipped with a visible note; a browser refusing `audio.play()` left the button on Pause with nothing playing → the run halts with a message; `object-fit: cover` could crop a signer → `contain`; `speechSynthesis.cancel()` on unmount could silence another panel → only the panel that is speaking cancels; the button after Pause says "Replay sentence", which is what it does; captions are grouped by their sentence index, not by time, and partly signed words show the unsigned part struck through after them. `app.py`: `show_timeline` is the one error boundary for every lane (a T5 that will not load becomes a message with the hint to switch engines); typed, weather and upload results live in session state so a tab switch or engine change no longer erases them; an upload is identified by the SHA-256 of its bytes, not name and size; widgets carry keys (the smoke tests select by key); the privacy caption says where the audio goes (back to the uploader's browser for playback, nowhere else). `asr.py`: model files download to `.part` and are renamed when complete; one lock around the model load so simultaneous first uploads cannot load two copies (the T5 loader has the same lock). `config.toml`: upload cap 25 → 12 MB, which bounds what the decoder must read before the 60 s check. Checked by hand in the browser (local run): Samoa item plays with verbatim captions and struck function words, sign label follows the clip (SUNDAY at 1.25×), Pause stops everything and nothing resumes on its own, Replay sentence and Restart behave, the waiting-for-the-interpreter state appears between sentences, and the typed lane renders "Don't ~~do~~", "30%" as 30 PERCENT, "2pm" as 2-P-M and "-5" as not available. Smoke tests: typed result survives a rerun; weather and engine failures render as messages. 95 tests. Codex review of this commit pending (usage limit); to run: `scripts/codex_review.sh <sha>`.
- Batch 3, tooling, docs and structure (Codex audit findings on `scripts/`, `training/`, CI, requirements, docs). Scripts: `build_lexicon.py` takes exactly `plan` or `fetch` (anything else prints usage and exits 2), writes media whole (`.part` then rename), drops a cached clip when the chosen source changed, keeps a concept's previous record when its fetch fails and exits 1, breaks candidate ties by identifier so a re-plan is stable, and no longer stores a `badge` in concepts.json (badges are decided at runtime); `build_demo_set.py` checks both locate scores and the range before cutting audio, and downloads whole; `measure_rss.py` reads the OS high-water mark (ru_maxrss on Linux, peak_wset on Windows) instead of the current RSS, and measures a T5 timeline when the export exists (334 MB with T5, Windows); `measure_clip_spans.py` (was `clip_durations.py`) and `make_contact_sheets.py` (was `review_frames.py`, since a mid-frame is a sheet, not a review) exit 1 on unreadable clips and the sheets are replaced as a set; `seed_vocab.py` (was `vocab_seed.py`) for verb-first naming; `evaluate_gloss.py` and `coverage_report.py` use the one recorded forecast fixture, the evaluation states when the T5 column is missing, carries the recorded decision instead of resetting to "pending", and states the training caveats (unpinned revisions, references truncated to 64 tokens); `codex_review.sh` accepts only a revision that names one commit, takes the answer between the CLI's marker and its token count (the answer was being printed twice), and silences line-ending warnings; `export_ct2.py` regenerates the tokenizer from the `--model` it was trained from. Requirements: opencv and torch pinned, `requirements-build.txt` no longer repeats what dev brings, "Kaggle only" wording corrected everywhere. `.dockerignore` added (secrets, environments, models, caches stay out of the image). Docs: plan §4 synchronisation row and pre-mortem marked superseded by ADR 0008, §5 tree relabelled as a maintained overview and completed (AGENTS.md, ignore files, script names), process table points at the real diagrams and runbook and no longer claims a red run cannot merge; deployment record marks the deployed Python version UNVERIFIED (host reported 3.14.7); TRD data model and honesty section describe verbatim captions, partly/missing, T5 coverage by concept and the number policy; test strategy rewritten to the suite as it is (100 tests, 98% line coverage, browser checks named as such); NOTICE separates shipped content from training inputs and from sources never used; README gives PowerShell and POSIX commands and the four badges. Rejected from the audit with reason: Signbank approval evidence per video (the entry page's published video is what is fetched; every clip was eyeballed on the contact sheets), verifying rights on hand-reviewed CATS overrides (reviewed with the archive page open; recorded in overrides.json), pinning transitive dependencies and the Docker base digest (Community Cloud installs from requirements.txt; a lock file adds tooling for no deployed effect), a Playwright browser test stage (developer's call, rule 4), splitting `tests/` into sub-folders (twelve files; flat is conventional at this size), moving the spike script into a sub-folder (one file), the User-Agent naming the public repository (it is the project's address, not personal data). Codex review of this commit pending (usage limit); to run: `scripts/codex_review.sh <sha>`.
- Codex reviews of the three audit commits ran after the usage limit reset (21:54). Applied: the panel showed a reused buffer's previous frame under the new label because seeking is asynchronous → the buffer is swapped in only on `seeked`; a failed clip in the middle of a fingerspelled number let the remaining digits play under the whole number's label → a failure ends the entry and shows the word as text with a note; stale `play()` rejections and cancel handles could touch a newer run → every completion is idempotent and a cancel handle is cleared only by its own operation; a stale text-hold timer could hide a newer run's text → token-checked; `halt()` left the button on "Pause" after a browser refused playback → the label is part of `halt()`; the footer read the lexicon outside the error boundary → guarded, with a smoke test that breaks `lexicon.load`; `build_lexicon.py` deleted a published clip before its replacement downloaded → the old file stays until the whole replacement is written; `ru_maxrss` is KiB on Linux, so the gate understated by 2.4% → `× 1024`; the CI memory gate ran without T5 → `ci.yml` sets `T5_RELEASE_URL` and caches the export, so the gate now covers all models; no tests for the build scripts → `tests/test_scripts.py` (excerpt location incl. weak ends and short excerpts, whole-file download, the CLI refusing unknown commands); T5 quantities were checked word by word, so "one hundred" glossed HUNDRED ONE or HUNDRED HUNDRED passed → the quantity words of a sentence must come out in the same order and count or every quantity in it is refused; the tokenizer kept only the U+0300 combining block → all five combining-mark blocks, tested one mark each; "20 - 30" as three words became a validated 20 and a refused -30 → a spaced range is joined into one refused token owned by its first word, and the sentence still ends on the last word consumed. 112 tests. Rejected: none this round.
- Second Codex round on the fix batch, applied: the 8 s load timer stayed armed during playback, so a clip longer than 8 s from load would have been reported failed → cleared once the seek lands; a late `seeked` after a timeout could still swap buffers → single-use handler that checks `done`; "20. - 30 people." was joined into one range across a sentence end → a range is never joined across `_ends_sentence`, and a carried sign belongs to its number's sentence (its onset starts the expression); the fetch replacement path had no test → `test_fetch_keeps_the_published_clip_until_its_replacement_is_complete` (failed replacement keeps the old bytes and the old record; success replaces both, drops the old measurements and sends the concept back to review). 113 tests.
- Rounds three and four: the three words of a spaced range now share the tokens, sentence and caption status of the word that owns the expression (a refused range is shown as text like any refusal; when nothing accounts for it under T5 all three are struck); `build_lexicon.py fetch` writes replacement footage next to the old file and renames it into place only after the manifests are written, so a crash between the two can leave at most old footage under a record already marked review, never new footage under an attested record; the panel gained a progress watchdog (4 s without a `timeupdate` while playing counts as a failed clip) so a stalled clip cannot hang a sentence. Rejected again with the same reason: a JS test harness for the playback state machine (developer's structure call); the paths were checked by hand instead: label swaps only on `seeked`, Pause/Replay/Restart, and the stall watchdog by inspection.
- Round five: the five hard-coded combining blocks missed marks such as Hebrew sheva (U+05B0), so `qְx` split into spellable fragments → the tokenizer's mark class is now built from Unicode categories at import (every M* code point, 0.1 s once), tested with Hebrew, Arabic and Devanagari marks; a run that stopped between writing the manifests and moving replacement footage would have left the manifest naming the new source over old footage, and the next run trusted it → a `.pending` marker is written when a replacement is staged and removed only when the footage is in place, so the next run fetches again instead of trusting; tested by reconstructing the crash state.
- Round six: T5 quantities were compared as one flat list, so "one hundred people and two cats" regrouped as ONE ... HUNDRED TWO passed → quantities are compared as groups (runs of consecutive quantity words) in order; a spaced en dash ("20 – 30") was not recognised as a range separator → all dash forms and "+" are, and the operator written in the source is the one kept ("20 + 30" → 20+30, refused, never 20-30); "+" inside an expression now keeps it whole in the tokenizer.
- Rounds seven and eight: a clip whose media ended before its out-point was reported as played → it counts as failed (the entry falls back to text), with a 50 ms tolerance (one frame plus the rounding of the recorded times; out_s never exceeds the duration, the lexicon loader checks that); `halt()` has a default button label and every caller passes one.
- Live check after c17a36c, first attempt: the panel raced through 18 signs in 10 s with the note "clip could not be loaded". Cause found by probing `video.play()` inside the Cloud iframe: `AbortError: video-only background media was paused to save power`, i.e. the browser pane used for checks was a hidden tab, and Chrome refuses to start muted video there. Not a defect of the app (a viewer's tab is visible); with the new panel such a refusal shows the word as text with a note instead of skipping it silently. The note text also showed the host was still serving the previous build at that moment.

## 2026-09-11
- Owner set Python 3.12 in the app's Advanced settings (takes effect at the next boot; the footer will confirm). The live sidebar shows "T5 export not present": `T5_RELEASE_URL` is not set in the Cloud secrets, owner action.
- Front-end round agreed (five changes, in order: panel poster frame instead of a black box, item card with coverage and signing time, keyboard control and badge legend, sign-speed control, sidebar and title tidy), plus two items from the `awesome-frontend-skills` list the developer asked me to check: Anthropic's `webapp-testing` skill (Python Playwright), which is the panel harness Codex kept asking for and fits the Python-only toolchain, and pbakaus/impeccable's design critique, to be run once the changes are in. The rest of that list is framework-specific (React, Vue, Angular, Next, Tailwind, auth, ORM) and does not apply.
- Browser harness added: `tests/browser/` with a session fixture that starts Streamlit on a free port and Playwright's headless Chromium (its autoplay policy lets `play()` resolve without a gesture); CSS locators pierce the component's shadow root. Nine tests: mount ready, label changes only with a decoded clip of that sign, Pause stops every media element and nothing resumes, Replay and Restart, the observed clip sequence equals the timeline's prescribed sequence with every sampled frame decoded, a clip aborted by `page.route` falls back to text and playback continues, a failed digit inside 975 takes the whole number to text and the remaining digit never plays, dropped and partly-signed words are visibly struck through (computed style), no horizontal scroll at 375 and 1440 px. Codex rounds on the harness: a launch failure is a skip only for a missing local browser and never in CI; the sequence test compares against the timeline rather than accepting any matching pair; Restart is proven by moving past the first sign first. `playwright==1.62.0` pinned in dev requirements; CI installs Chromium (`--with-deps`). 126 tests; the browser ones skip without Chromium. Reverses the earlier rejection recorded on 2026-09-10: the reason was a Node toolchain, and this harness has none.
- Harness rounds two and three: the sequence test now watches the whole first sentence to its end and compares every clip start (a rewound clock on the same clip counts as a start) with the prescribed sequence, so a stall, a repeat or a skip fails; the failed-digit test keeps watching through the text hold and into the next entry, so a late digit cannot slip by; a missing Playwright is a skip only locally, never in CI.
- Harness rounds four to seven: playback is now recorded by the browser rather than sampled: one record per `play()` with the label, the file, the time it started at, the number of seeks while playing and the time it stopped at (pause or ended, whichever first), so the sequence test asserts every clip of the first sentence started at its `in_s`, played through without a seek and stopped at or past `out_s` (0.05 s, the panel's own stop tolerance); Replay is proven to start the sentence over from its first clip after pausing well into it; the failed-digit test watches to the end of the item plus a second and asserts neither the failed digit nor the remaining one ever started under any label. A lesson recorded for the next person: the `pause` event lands after the panel has moved on, so a completion record must be tied to its own play, not to the label showing at the time.
- Harness rounds eight to fourteen (verdict merge): both bounds of every clip's stop time (at or past `out_s` − 0.03, no more than one timeupdate interval beyond it), start within 0.05 s of `in_s`, a playing clip must be visible with its label, the stop position is captured the moment the panel calls `pause()` (the queued pause event can land after a later seek on the same element), and the layout test checks the panel's own containers and controls, not just the document width; the element that plays must be the one on screen; narration starts are recorded too, so Pause is proven to cancel narration as well as clips.
- Front-end change 1 of 5: the panel used to open as a black box until Play. It now preloads the first clip and shows its first active frame, paused, with a "Press Play" hint (hidden once a run starts; absent when the first entry is text). The first `playClip` finds the same element already loaded and seeks from there, so nothing is fetched twice. Browser test: the mounted panel shows the first clip's frame at `in_s`, decoded and paused, with the hint, and the hint goes when Play is pressed. Codex: the preview showed a signer with no label → the first entry's gloss and badge are set when the frame is revealed, asserted in the test.
- Front-end change 2 of 5: an item card above the panel (`app.py::item_card`), four Streamlit metrics read from the timeline the viewer is about to watch: validated signs (coverage), fingerspelled share, projected signing time at the recorded rates (with the narration length and the pacing rule in the help text), and the sentence count. Same card on every lane. Smoke test asserts the four labels and value shapes; the browser test asserts the card sits above Play; both tests match each metric by label to the exact timeline value (Codex: a concatenated-text search could pass on a digit inside another number).
- Front-end change 3 of 5: keyboard control and a badge legend. Space plays or pauses and R restarts while focus is anywhere in the panel (the region is focusable with the same visible outline; a focused button keeps handling Space itself so nothing fires twice; both buttons declare `aria-keyshortcuts`). A caption under the ribbon spells out the four badges, the strike-through and the keys, so colour is never the only carrier. Accessibility report updated; unit test on the legend text, smoke test on the caption, browser test drives Space, Space, R and checks the shortcut attributes. Codex: a held key repeated the toggle and Ctrl+R was swallowed → autorepeat and modified keys are ignored (a held Space is still prevented from scrolling), a focused button keeps its own Space; all tested.
